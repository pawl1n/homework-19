spring:
  datasource:
    url: jdbc:postgresql://${PGHOST}:${PGPORT}/${PGDATABASE}
    username: ${PGUSER}
    driver-class-name: org.postgresql.Driver
    password: ${PGPASSWORD}
  jpa:
    open-in-view: false
